
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Exo+2:ital,wght@0,300;0,400;0,600;1,300&display=swap');

:root {
    --bg-deep:       #040b1a;
    --bg-mid:        #071228;
    --bg-card:       #0b1e3d;
    --accent-blue:   #1e90ff;
    --accent-cyan:   #00d4ff;
    --accent-gold:   #f0c040;
    --text-main:     #d0e8ff;
    --text-muted:    #7a9cc0;
    --border:        rgba(30, 144, 255, 0.25);
    --glow:          0 0 18px rgba(0, 212, 255, 0.45);
    --radius:        8px;
}

*, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

body {
    font-family: 'Exo 2', sans-serif;
    font-weight: 300;
    background-color: var(--bg-deep);
    color: var(--text-main);
    line-height: 1.75;

    /* Étoiles simulées via radial-gradient */
    background-image:
        radial-gradient(1px 1px at 10%  15%, rgba(255,255,255,0.7) 0%, transparent 100%),
        radial-gradient(1px 1px at 30%  60%, rgba(255,255,255,0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 55%  25%, rgba(255,255,255,0.6) 0%, transparent 100%),
        radial-gradient(1px 1px at 75%  80%, rgba(255,255,255,0.4) 0%, transparent 100%),
        radial-gradient(1px 1px at 90%  40%, rgba(255,255,255,0.7) 0%, transparent 100%),
        radial-gradient(2px 2px at 20%  90%, rgba(255,255,255,0.3) 0%, transparent 100%),
        radial-gradient(2px 2px at 65%  10%, rgba(255,255,255,0.4) 0%, transparent 100%),
        radial-gradient(ellipse at 50% 0%, rgba(30, 80, 160, 0.35) 0%, transparent 70%);
    background-size: 100% 100%;
    min-height: 100vh;
}

body > * {
    max-width: 860px;
    margin-left: auto;
    margin-right: auto;
    padding-left: 1.5rem;
    padding-right: 1.5rem;
}

h1 {
    font-family: 'Orbitron', sans-serif;
    font-weight: 900;
    font-size: clamp(2.4rem, 6vw, 4rem);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-align: center;
    padding: 2.5rem 0 0.5rem;
    background: linear-gradient(135deg, var(--accent-cyan) 0%, var(--accent-blue) 60%, var(--accent-gold) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: none; 
    filter: drop-shadow(0 0 22px rgba(0, 212, 255, 0.55));
}

nav {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 0.75rem 1.25rem;
    margin: 1.2rem auto 2rem;
    text-align: center;
}

nav p {
    font-size: 0.9rem;
    color: var(--text-muted);
    letter-spacing: 0.04em;
}

nav a {
    color: var(--accent-cyan);
    text-decoration: none;
    font-weight: 600;
    transition: color 0.2s, text-shadow 0.2s;
}

nav a:hover {
    color: var(--accent-gold);
    text-shadow: 0 0 8px var(--accent-gold);
}

section {
    background: var(--bg-card);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent-cyan);
    border-radius: var(--radius);
    padding: 2rem 2rem;
    margin-bottom: 2rem;
    box-shadow: var(--glow), inset 0 0 40px rgba(0, 50, 120, 0.15);
    animation: fadeUp 0.6s ease both;
}

section:nth-child(2) { animation-delay: 0.1s; }
section:nth-child(3) { animation-delay: 0.2s; }
section:nth-child(4) { animation-delay: 0.3s; }
section:nth-child(5) { animation-delay: 0.4s; }

@keyframes fadeUp {
    from { opacity: 0; transform: translateY(18px); }
    to   { opacity: 1; transform: translateY(0); }
}

h2 {
    font-family: 'Orbitron', sans-serif;
    font-weight: 700;
    font-size: 1.25rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--accent-cyan);
    margin-bottom: 1rem;
    padding-bottom: 0.5rem;
    border-bottom: 1px solid var(--border);
}

p {
    font-size: 0.975rem;
    color: var(--text-main);
    margin-bottom: 0.75rem;
}

a {
    color: var(--accent-blue);
    text-decoration: none;
    border-bottom: 1px dotted var(--accent-blue);
    transition: color 0.2s, border-color 0.2s;
}

a:hover {
    color: var(--accent-gold);
    border-color: var(--accent-gold);
}

ul {
    list-style: none;
    padding-left: 0;
    margin: 0.5rem 0 1rem;
}

ul li {
    padding: 0.45rem 0 0.45rem 1.6rem;
    position: relative;
    font-size: 0.95rem;
    color: var(--text-main);
    border-bottom: 1px solid rgba(30, 144, 255, 0.08);
}

ul li::before {
    content: '▹';
    position: absolute;
    left: 0;
    color: var(--accent-cyan);
    font-size: 0.9rem;
    top: 0.45rem;
}

ul li strong {
    color: var(--accent-gold);
    font-weight: 600;
}

em {
    color: var(--accent-cyan);
    font-style: italic;
}

#galerie a {
    display: inline-block;
    border: 2px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    transition: border-color 0.3s, box-shadow 0.3s, transform 0.3s;
    border-bottom: 2px solid var(--border);
}

#galerie a:hover {
    border-color: var(--accent-cyan);
    box-shadow: var(--glow);
    transform: scale(1.02);
}

#galerie img {
    display: block;
    max-width: 100%;
    height: auto;
    opacity: 0.88;
    transition: opacity 0.3s;
}

#galerie a:hover img {
    opacity: 1;
}

@media (max-width: 600px) {
    section {
        padding: 1.25rem 1rem;
    }

    h1 {
        font-size: 2rem;
    }
}
